// Copy this file to .env and fill values (for local testing only)
// On Vercel, set these in Project Settings > Environment Variables instead.

# Admin panel login
ADMIN_USERNAME=admin
ADMIN_PASSWORD=crown123

# MongoDB Atlas (free) - permanent storage for menu + bookings
# Example: mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
MONGODB_URI=
MONGODB_DB=mrcrown
